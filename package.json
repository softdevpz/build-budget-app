{
  "name": "build-budget-app",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:shared": "npm run build -w packages/shared",
    "dev:web": "npm run build:shared && npm run dev -w apps/web",
    "dev:api": "npm run build:shared && npm run start:dev -w apps/api",
    "build:web": "npm run build:shared && npm run build -w apps/web",
    "build:api": "npm run build:shared && npm run build -w apps/api",
    "test:api": "npm run build:shared && npm run test -w apps/api",
    "test:api:cov": "npm run build:shared && npm run test:cov -w apps/api",
    "test:api:e2e": "npm run build:shared && npm run test:e2e -w apps/api",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down"
  }
}
